# 仮想CPU ... レジスタと命令セット

## レジスタ

仮想CPUのレジスタは以下の通り。内部インタプリタ実行用のレジスタとスタックポインタ2本、プログラムカウンタ1本から成る。アキュムレータはない。スタックトップが演算対象となる。

Register|Description  
 |--|--|
 PC|プログラムカウンタ。仮想CPU命令を「フェッチ」「実行」してゆく。
 IP|インストラクションレジスタ。現在実行中のセカンダリワードの中で次の命令のアドレスを保持する。
 WA|ワードアドレスレジスタ。現在実行中のキーワードのアドレス、または、現在のキーワードのボディ位置の最初のコードのアドレスを保持する。
 CA|コードアドレスレジスタ。
 RS|リターンスタックレジスタ
 SP|データスタックポインタレジスタ

## 命令セット

仮想CPUの命令は1ワード命令がほとんどだが、リテラル系は2ワード、文字列リテラルは不定長(オペランドの文字列の長さによる)、分岐命令も2ワードである。

命令1つがForth処理系のプリミティブに対応しており、機械語(7000から始まる)それぞれの値がopcode.c のswitch case節につながる。

 |オペコード| | 
 |--|--|
 |0000-6FFF|無効命令(実行すると「例外：エラーメッセージが表示」される)
 |7000-7040|有効命令コード(下表参照)
 |7042-7FFF|無効命令
 |8000-FFFF|無条件分岐命令(相対オペランド、8000-BFFF:正方向、C000-FFFF:負方向)

現在、命令一覧は以下の通り。ほとんどの命令がForthのワードに対応しているので、それらについてはスタック操作も記載している。

 |機械語|ワード名|ニモニック|スタック操作|説明
 |--|--|--|--|--|
 |7000|nop|nop|( --- )|何もしない。最初から定義しておいたが、結局ここに至るまで1回も使っていない。
 |7001|halt|halt|( --- )|仮想CPUの実行を中断して、システムに返る(ターゲット環境異存)。`abort`の実装で使用している。
 |7002|colon1|(NA)| |内部インタプリタ用。IPをリターンスタックにプッシュしたのちにIPにWAを転送する。
 |7003|next1|(NA)| |内部インタプリタ用。IPの指す先をWAに転送し、IPをインクリメントする。
 |7004|run|(NA)| |内部インタプリタ用。WAの指す先をCAに転送し、WAをインクリメント後CAにジャンプする。間接ジャンプ。
|7005|startdoes|(NA)| |`does>`実装用。WAをデータスタックにプッシュ後、WAにPC+2を代入しrunに飛ぶ。
|7006|semi1|(NA)| |内部インタプリタ用。リターンスタックからIPにポップする。スレッドコードではSEMIがxtだが、その実処理の機械語がm_semi1である。
|7007|execute|execute|( xt --- )|スタックトップのxtが指すコードを実行する。テキストインタプリタで、切り出したワードを「実行」するときに使用している。
|7008|trap|trap|( xt --- )|「例外」を発生して仮想CPU実行を中断する。`abort`の実装で使用している。いくつかのワードで回復不能系のエラー発生の際に呼び出す。例えば、`'`(comma)ワードで、次の文字列が辞書で見つからなかった場合など。
|7009|dolit|dolit|( --- w )|直後のワードをスタックトップに置く。コロン定義中で数値をコンパイルする際にスレッドコードに書き込まれる。2ワード命令。
|700a|?branch|qbranch|( flag --- )|条件分岐命令。スタックトップが0の場合ジャンプする。直後のワードをジャンプ先アドレスとしてIPをそこに移す(スレッドコード内ジャンプ)。2ワード命令。
|700b|branch|branch|( --- )|無条件分岐命令。直後のワードをジャンプ先アドレスとしてIPをそこに移す(スレッドコード内ジャンプ)。2ワード命令。
|700c|c!|cdepo|( c addr --- )|アドレスaddrが指す先に1バイトcを書き込む。
|700d|!|depo|( w addr --- )|アドレスaddrが指す先に1ワードwを書き込む。
 |700e|c@|cfetch|( addr --- c )|アドレスaddrが指す先のメモリ1バイト分の値をスタックに積む。
 |700f|@|fetch|( addr --- w )|アドレスaddrが指す先のメモリ1ワード分の値をスタックに積む。
 |7010|rot|rot|( w1 w2 w3 --- w2 w3 w1 )|スタック上の3ワードを回す
 |7011|swap|swap|( w1 w2 --- w2 w1 )|スタック上の2ワードを上下入れ替える
 |7012|drop|drop|( w --- )|スタック最上ワードを捨てる
 |7013|over|over|( w1 w2 --- w1 w2 w1 )|スタック2つ目のワードをコピーしてスタックに置く。
 |7014|dup|dup|( w1 --- w1 w1 )|スタック最上ワードを複製して積む。
 |7015|rsp|rsp|( --- addr )|リターンスタックポインタの値をスタックに積む。
 |7016|r>|r2s|( --- w )|リターンスタックトップをポップしてスタックに積む。
 |7017|>r|s2r|( w --- )|スタックトップをポップしてリターンスタックに積む。
 |701a|not|not|( w --- ~w )|否定。スタックトップが0ならば(-1)(0xFFFF)を、非ゼロなら0をスタックに積む。
 |701b|xor|xor|( w1 w2 --- w1^w2 )|スタック上2ワードを xor する。
 |701c|or|or|( w1 w2 --- w1|w2 )|スタック上2ワードを or する。
 |701d|and|and|( w1 w2 --- w1&w2 )|スタック上2ワードを and する。
 |701e|/|div|( n1 n2  --- n1/n2 )|除算命令
 |701f|`*`|mul|( n1 n2 --- n1`*`n2)|乗算命令
 |7020|-|sub|( n1 n2 --- n1-n2 )|減算命令
 |7021|+|add|( n1 n2 --- n1+n2 )|加算命令
 |7022|>|gt|( n1 n2 --- t|f )|n1 < n2 ならばtrueをスタックトップに積む。
 |7023|/mod|slashmod|( n1 n2 --- n1/n2 mod(n1,n2) )|除算なのだが、商と余りをスタックに積んで帰る。
 |7024|emit|emit|( c --- )|1バイトを端末出力する。
 |7025|:|colondef| |コロン定義の開始。
 |7026|;|semidef| |コロン定義の終了。即値ワード。C言語プリミティブだが、のちにForth定義のワードに上書きされる。
 |7027|;;|semi2def| |(semidefと同じ、`;`を再定義する際にのみ使用する)
 |7028|dd|dd|( addr --- )|ワード逆アセンブラ、デバッグ用。スタックトップaddrを含むワード定義をダンプする。
 |7029|+rsp|add_rsp|( n --- )|RSPにスタックトップの値を加算する。リターンスタック操作用。
 |702a|sp@|sp_at| |SPの値をスタックに置く。
 |702b|lnum|lnum| |入力テキストの行番号(lnum)をスタックに積む。C言語テキストインタプリタ中で、引数ファイルが切り替わるたびに`lnum`はリセットされる。
 |702c|getline|getline|( n addr --- -1 or 0 )|1行入力ルーチン。
 |702d|outer|outer| |テキスト入力先が、外部ファイルからか、キー入力からかを示すフラグ`outer`の値を返す。trueであればファイル入力、falseであればキー入力。
 |702e|d+|dadd|( d1 d2 n --- d(=d+n) )|倍長整数と単長整数の加算。(d1<<16+d2) + (n3<<16+n4)
 |702f|m+|madd|( d1 d2 m2 --- d3 d4 )|倍長整数を単長整数で乗算のあと別の単長整数で除算。(d1<<16+d2) * m2 / m3
 |7030|m*/|mmuldiv|( d1 d2 m2 m3 --- d3 d4 )| |倍長整数を単長整数で乗算のあと別の単長整数で除算。(d1<<16+d2) * m2 / m3
 |7031|d<|dlt|( d1 d2 d3 d4 --- t/f )|倍長整数どうしの比較。
 |7032|u*|umul|( d1 d2 m1 --- d3 d4 )|倍長整数に単長整数を乗算。
 |7033|rp!|rsp_reset|( --- )|リターンスタックポインタを初期化。初期値は辞書先頭に格納されている値である。
 |7034|sp!|sp_reset|( --- )|スタックポインタを初期化。初期値は辞書先頭に格納されている値である。
 |7035|s_dolit|s_dolit|( --- )| |文字列リテラル命令。スレッドコード中に文字列定数を置くことができる。
 |7037|dictdump|dictdump|( begin end last --- )| |辞書イメージをファイルに書き出す。ファイル名のデフォルト値は"forth.bin"、ファイル名を変更するためには、`cpnr`起動時に'-o'オプションとともに指定する。
 |7038|'|quote|( --- xt )|次のワード文字列で辞書を検索し、見つかったエントリのxtをスタックに積む。
 |7039|key|key|( --- c )|キー入力を待ち、得た値1文字をスタックに積む。Linux実装ではraw modeで1文字freadして返す
 |703b|bye|bye|( --- )|テキストインタプリタ実行を終了し、システムに帰る。Linux版では`cpnr`から抜け出しシェルコマンド入力待ちに戻る。
 |703c|[compile]|ccompile| |入力ストリームの次のワードのxtをコンパイルする。コンパイル中に入力ストリームからの読み込み・検索を行うため即値ワードになっている。
 |703d|dump|dump|( addr n --- )|指定されたアドレスからnワードダンプする。デバッグ用。
 |703e|.ps|dotps|( c --- )|スタック上の1文字を印字した後にスタック内部をダンプする。デバッグ用。
 |703f|exch|exch| |スタックトップとWAレジスタを入れ替える。Forthワードではない機械語命令。内部インタプリタの`DODOES`ルーチンで使用している。ターゲットCPU用には相当する機械語ルーチンに置き換える。
 |7040|call|call| |機械語コール命令。戻り番地をデータスタックにプッシュした後、オペランド(次のワード)の値をアドレスとしてそこにジャンプする。`does>`実装で使用している。ターゲットCPUには相当する機械語ルーチンに置き換える。

